# Secrets — nunca enviar ao GitHub
config.json
valor_cookies.json

# Arquivos gerados (ficam localmente, não no repo de scripts)
noticias_valor_*.json
texto_episodio_*.txt
audio/*.mp3
pipeline.log
automacao.log
artigo_diagnostico.html

# Backups locais do perfil (gerados por aprender_perfil.py) — não versionar
perfil_interesses.backup-*.json

# Caixa de entrada local de manchetes (o fluxo oficial é a Planilha Google)
manchetes_favoritas.txt

# Python
__pycache__/
*.pyc
*.pyo
.env

# macOS
.DS_Store
